# Production environment — used by `npm run build` to bake the correct URL into the client bundle.
# Fill in YOUR actual domain before building, then re-run `npm run build`.

NEXT_PUBLIC_SOCKET_URL=https://yourdomain.com
